# AWS API Gateway Assessment

This project contains a serverless API built with AWS API Gateway, Lambda, Cognito, and CloudFormation.

Implementation and deployment details will be added as the project is completed.